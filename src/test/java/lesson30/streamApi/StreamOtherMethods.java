package lesson30.streamApi;

import java.util.List;
import java.util.stream.Stream;

public class StreamOtherMethods {
    public static void main(String[] args) {

        /*List<Integer> sorted = Stream.of(1, 4, 3).sorted().toList();
        sorted.forEach(System.out::println);

        List<Integer> limit = Stream.of(1, 4, 3).limit(1).toList();
        limit.forEach(System.out::println);

        List<Integer> distinct = Stream.of(1, 3, 3).distinct().toList();
        distinct.forEach(System.out::println);

        List<Integer> skip = Stream.of(1, 3, 3).skip(1).toList();
        skip.forEach(System.out::println);*/
    }
}
